- 所有测试文件均通过 `sys.path.insert` 动态将 `mcp-server` 目录加入搜索路径以支持模块导入。
- 每个测试脚本均包含 `if __name__ == "__main__":` 入口块，允许在不依赖 pytest 的情况下直接运行全部用例。
- 在涉及 LLM 调用的测试中，普遍采用注入 `fake_generate` 或 `monkeypatch` 的方式模拟生成函数以实现逻辑隔离。