- 采用 MVVM 架构模式，将业务逻辑（如 SessionReader, EnhanceClient）与视图状态（AppState）及 UI 组件（IslandView）严格分离。
- 耗时 I/O 操作（如会话列表读取、上下文解析）统一封装在 `Task.detached` 中执行，确保主线程 UI 交互的流畅性。
- 遵循深色主题设计规范，定义统一的 `Theme` 枚举管理色彩常量，确保卡片背景与系统刘海黑色区域视觉融合。