- 入口脚本 `context-assembly.py` 对比展示了基于 `PromptContext` 的结构化组装与传统的自由字符串拼接逻辑。
- `enhance-next-turn.py` 作为命令行工具，通过读取 JSON 配置文件（如 `next-turn-context.json`）动态构建上下文并调用增强接口。
- 模块依赖 `mcp-server` 目录下的 `context_packaging` 和 `enhance` 核心库，体现了从数据建模到提示词优化的调用链路。