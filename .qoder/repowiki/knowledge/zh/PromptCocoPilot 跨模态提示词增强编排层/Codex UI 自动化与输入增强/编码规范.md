- 采用依赖注入模式，将文件系统操作（如 `fs`）和进程执行（如 `execFileSync`）作为参数传入函数，以便于单元测试中模拟环境。
- 使用多策略降级机制进行资源发现，例如在获取调试端口时依次尝试环境变量、状态文件读取及已知端口扫描。
- 在注入脚本中通过全局变量（如 `window.__promptCocoPilotCodexPendingRequests`）建立渲染进程与 Node.js 守护进程之间的异步请求队列。