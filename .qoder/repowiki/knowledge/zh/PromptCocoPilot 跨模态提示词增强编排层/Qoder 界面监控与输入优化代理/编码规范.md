- 采用工厂函数模式生成动态脚本字符串（如 `createOptimizeInputObserverScript`），以便在注入前自定义绑定名称和版本标识。
- 所有异步资源获取（如 DevTools 端口、WebSocket 连接）均包含指数退避的重连机制（`runDaemon` 中的 `delayMs` 逻辑）。
- 使用全局唯一标识符（如 `__promptCocoPilotQoder...`）在浏览器上下文中管理状态，避免与宿主应用的全局变量冲突。