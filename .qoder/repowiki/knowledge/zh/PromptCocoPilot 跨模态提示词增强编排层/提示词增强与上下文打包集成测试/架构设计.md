- **分层测试架构**：底层（`test_enhance.py`）验证核心重写逻辑与清理规则；中层（`test_context_packaging.py`, `multi_round_smoke.py`）覆盖对话截断、代码事实去重及预算控制；上层（`test_server_tool.py`, `test_http_button_server.py`）确保 MCP 协议与 HTTP API 的参数映射一致性。
- **端到端闭环**：通过 `test_real_usage.py` 加载外部 Dashscope API Key 进行真实模型评估，形成从单元测试到集成的完整链路。
- **对抗性边界测试**：利用 `edge_round_smoke.py` 针对纯 ASCII/中文混合估算、空输入退化及长文本截断算法进行专项压力测试。