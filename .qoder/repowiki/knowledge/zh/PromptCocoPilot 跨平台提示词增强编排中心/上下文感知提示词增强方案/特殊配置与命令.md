- 配置 MCP: 在 `claude_desktop_config.json` 中添加 `prompt-enhancer` 服务器，指定 `python3` 及 `server.py` 绝对路径。
- 启动测试: 运行 `python3 mcp-server/http_server.py --host 127.0.0.1 --port 8765` 以启用本地 HTTP 接口供 UI 按钮调用。
- 技能安装: 将 `skill/SKILL.md` 复制至 `.claude/skills/prompt-enhancer/` 目录以启用自动引导。